<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>khatabook</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gradient-to-br from-blue-50 to-blue-200 min-h-screen">
    <div class="main w-full min-h-screen flex flex-col items-center">
      <nav class="w-full flex justify-between items-center py-6 px-12 bg-white shadow-md">
        <div>
          <h1 class="text-3xl font-bold tracking-tight">
            <span class="text-red-700">Khata</span><span class="text-blue-700">book_</span>
          </h1>
        </div>
        <div class="flex gap-8">
          <a class="text-lg font-medium text-gray-700 hover:text-blue-600 transition" href="/">Home</a>
          <a class="text-lg font-medium text-gray-700 hover:text-orange-600 transition" href="/create">
            Create new <span class="text-orange-700">Hisaab</span>
          </a>
        </div>
      </nav>
      <section class="w-full max-w-2xl mt-12 bg-white rounded-2xl shadow-lg p-10">
        <h1 class="text-2xl font-semibold mb-6 text-gray-800">
          You are editing hisaab of
          <span class="text-blue-500"><%= filename %></span> date
        </h1>
        <form action="/update/<%= filename%>" method="post" class="flex flex-col gap-6">
          <textarea
            class="resize-none w-full rounded-lg min-h-[300px] bg-zinc-100 p-4 text-gray-700 border border-blue-200 focus:outline-none focus:ring-2 focus:ring-blue-400 transition"
            placeholder="Shopping or kharchaa aap yha likhoo"
            name="content"
            id=""
          ><%= fileData %></textarea>
          <input
            class="self-end px-8 py-3 bg-gradient-to-r from-blue-500 to-blue-600 text-white font-semibold rounded-lg shadow hover:from-blue-600 hover:to-blue-700 transition cursor-pointer"
            type="submit"
            value="Update Hisaab"
          />
        </form>
      </section>
    </div>
  </body>
</html>
