<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>khatabook</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="main h-screen w-screen bg-white-600">
      <div class="nav flex justify-between border-b-2 p-[2vw] px-12">
        <div class="nav-part1">
          <h1 class="text-2xl font-medium">
            <span class="text-red-800">Khata</span>book_
          </h1>
        </div>
        <div class="nav-part2 flex gap-12 mr-12 mt-[5px]">
          <a class="text-md font-medium" href="/">Home</a>
          <a class="text-md font-medium" href="/create"
            >Create new <span class="text-orange-800">Hisaab</span></a
          >
        </div>
      </div>
      <div class="hisaabs px-12 mt-4 text-2xl">
        <h1 class="font-semibold">All Hisaab Kitaab...</h1>
      </div>
      <div class="hissab-box ml-[40px] mt-4 gap-3 flex flex-col">
        <% if(files.length>0){ %> <% files.forEach(function(file){ %>
        <div
          class="naya-hisaabbox w-1/3 bg-blue-600 rounded-md ml-2 py-2 px-4 flex items-center justify-between gap-[10vw]"
        >
          <div class="left flex gap-4">
            <h3 class="text-white"><%=file.split(".") [0]%></h3>
            <a
              class="w-6 h-6 text-center rounded-full bg-white"
              href="/hisaab/<%= file %> "
              ><i class="ri-arrow-right-line"></i
            ></a>
          </div>
          <div class="right flex gap-3">
            <a class="text-[20px]" href="/edit/<%= file %>"
              ><i class="ri-pencil-line"></i
            ></a>
            <a
              class="w-8 h-8 p-[4px] text-center rounded-full bg-white"
              href="/delete/<%= file%>"
              ><i class="text-red-800 ri-delete-bin-5-line"></i
            ></a>
          </div>
        </div>
        <% }) %> <% }else{ %>
        <h3 class="text-red-400 ml-1 text-2xl">
          Nothing to see create file to see something....
        </h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
